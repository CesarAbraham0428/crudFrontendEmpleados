import { EmpleadoActividad } from './empleado-actividad.model';

describe('EmpleadoActividad', () => {
  it('should create an instance', () => {
    expect(new EmpleadoActividad()).toBeTruthy();
  });
});
